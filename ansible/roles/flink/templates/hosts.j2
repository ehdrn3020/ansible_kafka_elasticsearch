127.0.0.1   localhost localhost.localdomain
::1         localhost6 localhost6.localdomain6

{% for host in groups['flink_masters'] + groups['flink_workers'] %}
{{ hostvars[host].ansible_host | default(host) }} {{ host }}
{% endfor %}
